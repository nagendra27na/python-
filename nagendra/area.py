#pogram which accepts the  radius of circle from the user  and compute the  area

from  math import pi

r=float(input("Enter radius of circle : "))

print("Area of the circle is: "+ str(pi* r**2))
